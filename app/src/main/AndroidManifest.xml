<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hashedin.marchantapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ViewActivity.RedeemResponseActivity"></activity>
        <activity android:name=".ViewActivity.RedeemActivity" />
        <activity android:name=".ViewActivity.LoginActivity" />
        <activity android:name=".ViewActivity.QRCodeScannerActivity" />
        <activity android:name=".ViewActivity.QRCodeGeneratorActivity" />
        <activity android:name=".ViewActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>